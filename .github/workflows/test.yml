name: Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  unit-tests:
    name: Unit Tests
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.21'

    - name: Ensure Local Git Config Isolation
      run: git config --global --add safe.directory "*"

    - name: Run Unit Tests
      run: go test -v ./...

  integration-tests:
    name: Integration Tests (Gemini)
    runs-on: ubuntu-latest

    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.21'

    - name: Run Integration Tests
      env:
        GEMINI_KEY: ${{ secrets.GEMINI_KEY }}
      # Check if the secret is set in the shell to avoid GitHub Actions YAML context limitations
      run: |
        if [ -z "$GEMINI_KEY" ]; then
          echo "⚠️ GEMINI_KEY secret is not configured. Skipping integration tests."
          exit 0
        fi
        echo "GEMINI_KEY=${GEMINI_KEY}" > .env
        go test -v -tags integration ./tests/ -run TestGemini -timeout 120s
